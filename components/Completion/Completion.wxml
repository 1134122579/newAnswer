<view class="fill-item">
   <block wx:for="{{titleArr}}" wx:key="index">
     <text wx:if="{{item !== '#'}}">{{item}}</text>
     <!-- <input wx:else class="fill-input" style="width:{{maxlength*36}}rpx" maxlength="{{maxlength}}"  data-value="{{index}}"   bindinput="fnInput"  disabled="{{disabled}}" /> -->
     <input wx:else class="fill-input" style="width:{{maxlength*36}}rpx"   data-value="{{index}}"   bindinput="fnInput"  disabled="{{disabled}}" />
   </block>
</view>